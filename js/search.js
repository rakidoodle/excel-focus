const Search={renderResults(query){const el=document.getElementById('searchResults'),q=query.trim().toLowerCase();el.hidden=!q;if(!q)return;const lessons=getAllLessons().filter(l=>(l.title+' '+l.levelTitle).toLowerCase().includes(q)).slice(0,7),formulas=Formulas.filter(f=>(f.name+' '+f.desc).toLowerCase().includes(q)).slice(0,3),projects=Projects.filter(p=>p.title.toLowerCase().includes(q)).slice(0,3);el.innerHTML=[...lessons.map(l=>`<a href="#learn/${l.levelId}/${l.id}">${escapeHTML(l.title)}<small>Lesson · ${escapeHTML(l.levelTitle)}</small></a>`),...formulas.map(f=>`<a href="#formulas" onclick="event.preventDefault();Components.pendingFormula='${escapeHTML(f.name)}';App.navigate('formulas')">${escapeHTML(f.name)}<small>Function reference</small></a>`),...projects.map(p=>`<a href="#projects/${p.id}">${escapeHTML(p.title)}<small>Project</small></a>`)].join('')||'<p class="empty-state">No matching lessons or formulas. Try a skill such as “lookup”, “dates” or “budget”.</p>';}};
