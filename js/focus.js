const Focus={timerInterval:null,secondsRemaining:0,level:null,lesson:null,
 enter(level,id){const l=getLesson(level,id);if(!l)return;this.stopTimer();this.level=level;this.lesson=id;document.getElementById('focusLessonTitle').textContent=l.title;document.getElementById('focusContent').innerHTML=l.content;const lv=getLevel(level);document.getElementById('focusProgress').textContent=`${lv.lessons.findIndex(x=>x.id===id)+1} / ${lv.lessons.length} lessons`;const minutes=Store.getSettings().focusTimer??25;this.secondsRemaining=minutes*60;this.updateTimerDisplay();const dialog=document.getElementById('focusOverlay');if(!dialog.open)dialog.showModal();dialog.onclose=()=>this.stopTimer();Store.saveFocusState({levelId:level,lessonId:id});if(minutes>0)this.startTimer();},
 startTimer(){this.stopTimer();this.timerInterval=setInterval(()=>{this.secondsRemaining=Math.max(0,this.secondsRemaining-1);this.updateTimerDisplay();if(!this.secondsRemaining){this.stopTimer();document.getElementById('focusTimer').textContent='Time for a break';}},1000);},
 stopTimer(){clearInterval(this.timerInterval);this.timerInterval=null;},updateTimerDisplay(){document.getElementById('focusTimer').textContent=this.secondsRemaining?`${Math.floor(this.secondsRemaining/60)}:${String(this.secondsRemaining%60).padStart(2,'0')}`:'Timer off';},
 continue(){Store.completeLesson(this.level,this.lesson);const lv=getLevel(this.level),i=lv.lessons.findIndex(l=>l.id===this.lesson),next=lv.lessons[i+1];if(next)this.enter(this.level,next.id);else{this.exit();App.navigate('learn',this.level);}},exit(){this.stopTimer();document.getElementById('focusOverlay').close();}
};
