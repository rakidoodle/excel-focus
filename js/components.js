const Components={
 heading(kicker,title,description='',action=''){return `<div class="page-heading"><div><p class="eyebrow">${kicker}</p><h1>${title}</h1></div>${action}</div>${description?`<p class="intro">${description}</p>`:''}`;},
 bookmarkBtn(type,id){const saved=Store.isBookmarked(type,id);return `<button class="bookmark-btn ${saved?'bookmarked':''}" onclick="App.toggleBookmark('${type}','${id}',this)" aria-label="${saved?'Remove bookmark':'Bookmark'}" aria-pressed="${saved}">${Icons.get('bookmark')}</button>`;},
 done(level){return level.lessons.filter(l=>Store.getProgress()[level.id]?.lessons?.[l.id]?.completed).length;},
 counts(){const lessons=getAllLessons(),progress=Store.getProgress();return {total:lessons.length,done:lessons.filter(l=>progress[l.levelId]?.lessons?.[l.id]?.completed).length};},
 resume(){const saved=Store.getFocusState();return saved&&getLesson(saved.levelId,saved.lessonId)?{level:getLevel(saved.levelId),lesson:getLesson(saved.levelId,saved.lessonId)}:{level:AcademyTracks[0],lesson:AcademyTracks[0].lessons[0]};},
 home(){const count=this.counts();document.getElementById('view-home').innerHTML=`<div class="welcome-layout"><div><p class="eyebrow">A WORKSPACE FOR LEARNING EXCEL</p><h1>Make sense of the cells.<br>Then make them work.</h1><p class="intro">Learn one useful idea. Try it in a real worksheet. Build the confidence to take on the next.</p><div class="actions"><a class="btn btn-primary" href="#learn/${AcademyTracks[0].id}/${AcademyTracks[0].lessons[0].id}">Start with the basics ${Icons.get('arrow')}</a><a class="btn btn-quiet" href="#assessment">Find my starting point</a></div><p class="small" style="margin-top:18px">No account. No streak pressure. Your pace.</p></div><figure style="margin:0"><img class="editorial-art" src="assets/workbook-editorial.webp" width="1200" height="800" alt="Original editorial illustration of a selected cell range, structured data and a small bar chart on warm paper"><figcaption class="art-caption">FROM RAW DATA TO A CLEARER PICTURE.</figcaption></figure></div><div class="method-strip"><article><span class="mono">01 / UNDERSTAND</span><h3>One idea at a time.</h3><p>${count.total} focused lessons, from your first cell to professional workbook design.</p></article><article><span class="mono">02 / EXPERIMENT</span><h3>Keep your hands on the data.</h3><p>${Exercises.length} checked workbook exercises in a working spreadsheet. Mistakes welcome.</p></article><article><span class="mono">03 / APPLY</span><h3>Make something useful.</h3><p>${Projects.length} guided projects using realistic, fictional business datasets.</p></article></div><div class="section-heading"><h2>A path with room to grow.</h2><a class="text-link" href="#curriculum">Explore all 24 tracks ${Icons.get('arrow')}</a></div>${AcademyTracks.slice(0,4).map((t,i)=>this.trackPreview(t,i)).join('')}<div class="practice-callout"><div><p class="eyebrow">THE WORKBOOK LAB</p><h3>Reading is only half the lesson.</h3><p>Edit cells, build formulas and check your answer—without leaving your learning workspace.</p></div><button class="btn btn-primary" onclick="Lab.open('ex-001')">Try your first exercise ${Icons.get('arrow')}</button></div>`;},
 trackPreview(t,i){return `<div class="activity-row"><span class="row-number">${String(i+1).padStart(2,'0')}</span><div class="row-main"><strong>${escapeHTML(t.title)}</strong><p>${t.lessons.length} lessons · ${t.difficulty} · ${this.done(t)} complete</p></div><a class="text-link" href="#learn/${t.id}" aria-label="Open ${escapeHTML(t.title)}">Explore ${Icons.get('arrow')}</a></div>`;},
 renderDashboard(){const {level,lesson}=this.resume(),{total,done}=this.counts(),weak=getAllLessons().filter(l=>Store.getQuizResult(l.levelId,l.id)?.score===0).slice(0,3),bookmarks=Store.getBookmarks();document.getElementById('view-dashboard').innerHTML=this.heading('YOUR NEXT CHAPTER','A little progress, every time.','Pick up where you left off, or make room for a little practice.',`<a class="btn" href="#curriculum">Browse curriculum</a>`)+`<div class="dashboard-layout"><div><section class="continue-panel"><p class="eyebrow">${Store.getFocusState()?'CONTINUE LEARNING':'A GOOD PLACE TO START'}</p><h2>${escapeHTML(lesson.title)}</h2><p class="small">${escapeHTML(level.title)} · ${lesson.duration}</p><div class="actions"><a class="btn btn-primary" href="#learn/${level.id}/${lesson.id}">Open lesson ${Icons.get('arrow')}</a><button class="btn btn-quiet" onclick="Focus.enter('${level.id}','${lesson.id}')">${Icons.get('focus')} Focus</button></div><div class="corner-grid" aria-hidden="true"></div></section><div class="section-heading"><h2>Put it into practice.</h2><a class="text-link" href="#practice">All exercises ${Icons.get('arrow')}</a></div><div class="activity-row"><span>${Icons.get('lab')}</span><div class="row-main"><strong>${Lab.workbook?.exercise?escapeHTML(Exercises.find(e=>e.id===Lab.workbook.exercise)?.title||'Your workbook'):'Your open workbook'}</strong><p>${escapeHTML(Lab.workbook?.title||'A blank space for your next idea')}</p></div><button class="btn btn-sm" onclick="Lab.open()">Open</button></div><div class="activity-row"><span>${Icons.get('practice')}</span><div class="row-main"><strong>A short practice session</strong><p>One question, a realistic dataset, a working formula.</p></div><button class="btn btn-sm" onclick="App.startDailyChallenge()">Try it</button></div>${Lab.workbook?.project?`<div class="activity-row"><div class="row-main"><strong>Current project</strong><p>${escapeHTML(Projects.find(p=>p.id===Lab.workbook.project)?.title)}</p></div><a class="btn btn-sm" href="#projects/${Lab.workbook.project}">Continue</a></div>`:''}</div><aside class="dashboard-aside"><p class="eyebrow">YOUR LEARNING RECORD</p><div class="progress-number">${done}<span> / ${total} lessons</span></div><div class="progress-bar"><div class="progress-fill" style="width:${done/total*100}%"></div></div><p class="small">Progress stays on this device. No deadlines, no lost streaks.</p><a class="text-link" href="#progress">View your skill map ${Icons.get('arrow')}</a><div class="section-heading"><h3>Worth revisiting</h3></div>${weak.length?weak.map(l=>`<a class="lesson-link" href="#learn/${l.levelId}/${l.id}">${escapeHTML(l.title)} ${Icons.get('arrow')}</a>`).join(''):'<p class="small">As you answer checkpoints, concepts worth another look will appear here.</p>'}<div class="section-heading"><h3>Saved for later</h3><span class="small">${bookmarks.length}</span></div>${bookmarks.length?this.savedItems(bookmarks.slice(0,3)):'<p class="small">Bookmark a lesson or function to keep it close.</p>'}</aside></div><div class="section-heading"><h2>Your learning path</h2><span class="small">START WITH THE FOUNDATIONS</span></div>${AcademyTracks.slice(0,4).map((t,i)=>this.trackPreview(t,i)).join('')}`;},
 curriculum(){document.getElementById('view-curriculum').innerHTML=this.heading('24 TRACKS / ONE CLEAR JOURNEY','Build your spreadsheet fluency.','Start at the beginning, or go straight to the skill you need. Prerequisites are guidance, never locked gates.')+`<div class="filter-bar"><label class="sr-only" for="trackSearch">Find a track or lesson</label><input id="trackSearch" placeholder="Find a track or lesson…" oninput="Components.filterTracks(this.value)"><span class="filter-count">${AcademyTracks.reduce((n,t)=>n+t.lessons.length,0)} MICRO-LESSONS</span></div><div id="trackList">${this.trackRows(AcademyTracks)}</div><details style="margin-top:24px"><summary>Original course lessons · preserved for returning learners</summary>${Curriculum.filter(t=>!t.modern).map(t=>`<a class="lesson-link" href="#learn/${t.id}">${escapeHTML(t.title)}<small>${t.lessons.length} lessons</small></a>`).join('')}</details>`;},
 trackRows(tracks){return tracks.map(t=>`<details class="track-row"><summary><span class="row-number">${String(t.number).padStart(2,'0')}</span><div class="row-main"><h3>${escapeHTML(t.title)}</h3><p>${t.lessons.length} lessons · approximately ${t.lessons.length*5} minutes · ${this.done(t)} complete</p></div><span class="badge">${t.difficulty}</span></summary><div class="track-detail"><p class="small">Suggested prerequisite: ${escapeHTML(t.prerequisite)}</p><p>${escapeHTML(t.description)}</p>${this.lessonList(t)}</div></details>`).join('')||'<p class="empty-state">No matching tracks. Try “formula”, “data” or “chart”.</p>';},
 filterTracks(q){q=q.toLowerCase();document.getElementById('trackList').innerHTML=this.trackRows(AcademyTracks.filter(t=>(t.title+' '+t.lessons.map(l=>l.title).join(' ')).toLowerCase().includes(q)));},
 lessonList(t){return `<ol class="lesson-list">${t.lessons.map((l,i)=>`<li><a class="lesson-link" href="#learn/${t.id}/${l.id}"><span><span class="tag">${Store.getProgress()[t.id]?.lessons?.[l.id]?.completed?'✓':String(i+1).padStart(2,'0')}</span> &nbsp;${escapeHTML(l.title)}</span><small>${l.duration} ${l.exercise?'· Lab':''}</small></a></li>`).join('')}</ol>`;},
 renderLearn(id){const t=getLevel(id);if(!t){App.navigate('curriculum');return;}document.getElementById('learnContent').innerHTML=`<a class="text-link" href="#curriculum">← All tracks</a>`+this.heading('LEARNING TRACK',escapeHTML(t.title),escapeHTML(t.description))+`<p class="small">${this.done(t)} of ${t.lessons.length} lessons complete</p><div class="progress-bar"><div class="progress-fill" style="width:${this.done(t)/t.lessons.length*100}%"></div></div>${this.lessonList(t)}`;},
 renderLesson(levelId,id){const l=getLesson(levelId,id),t=getLevel(levelId);if(!l)return this.renderLearn(levelId);App.currentLevel=levelId;App.currentLesson=id;Store.saveFocusState({levelId,lessonId:id});const i=t.lessons.indexOf(l),next=t.lessons[i+1],prev=t.lessons[i-1],done=Store.getProgress()[levelId]?.lessons?.[id]?.completed;document.getElementById('learnContent').innerHTML=`<div class="lesson-meta"><a href="#learn/${levelId}">← ${escapeHTML(t.title)}</a><span>${i+1} / ${t.lessons.length}</span><span>${l.duration}</span>${this.bookmarkBtn('lesson',id)}</div><div class="lesson-layout"><article class="lesson-body"><h1>${escapeHTML(l.title)}</h1>${l.objective?`<div class="lesson-objective"><strong>Your objective</strong><br>${escapeHTML(l.objective)}</div>`:''}${l.concept?`<h2>The idea</h2><p>${escapeHTML(l.concept)}</p><h2>See it in context</h2>${l.example.startsWith('=')?`<pre>${escapeHTML(l.example)}</pre>`:`<p>${escapeHTML(l.example)}</p>`}<h2>Try it</h2><p>${escapeHTML(l.task)}</p><details><summary>A common mistake to avoid</summary><p>${escapeHTML(l.mistake)}</p></details>`:l.content}${l.exercise?`<div class="practice-callout"><div><p class="eyebrow">PRACTICE / WORKBOOK LAB</p><h3>${escapeHTML(Exercises.find(e=>e.id===l.exercise)?.title)}</h3><p>Known seed data. A clear target. Feedback on your answer.</p></div><button class="btn btn-primary" onclick="Lab.open('${l.exercise}')">Open exercise ${Icons.get('arrow')}</button></div>`:l.desktop?'<p class="feedback">This lesson uses desktop Excel. The Lab does not simulate this advanced feature. Download a dataset from a project to follow along in Excel.</p>':''}<div id="quizContainer"></div><details><summary>Your notes</summary><label class="sr-only" for="lessonNotes">Notes for this lesson</label><textarea id="lessonNotes" oninput="Store.saveNote('${id}',this.value)" placeholder="What clicked? What would you like to revisit?">${escapeHTML(Store.getNote(id))}</textarea><p class="small">Saved automatically on this device.</p></details><div class="lesson-footer">${prev?`<a class="btn" href="#learn/${levelId}/${prev.id}">← Previous</a>`:'<span></span>'}<button class="btn ${done?'':'btn-primary'}" onclick="App.markComplete('${levelId}','${id}')">${done?'✓ Completed':'Mark complete'}</button>${next?`<a class="btn" href="#learn/${levelId}/${next.id}">Next lesson →</a>`:`<a class="btn" href="#curriculum">Back to curriculum →</a>`}</div></article><aside class="lesson-aside"><p class="eyebrow">MAKE ROOM TO LEARN</p><h3>One idea is enough.</h3><p>Work through the example, try it yourself, then check your understanding.</p><button class="btn" onclick="Focus.enter('${levelId}','${id}')">${Icons.get('focus')} Focus mode</button><button class="btn btn-quiet" onclick="App.confusionHelp('${levelId}','${id}')">Break it down</button><hr style="border:0;border-top:1px solid var(--color-border);margin:24px 0"><p class="eyebrow">NEXT IN THIS TRACK</p><p>${escapeHTML(next?.title||'Track complete. Choose your next area of practice.')}</p></aside></div>`;if(l.quiz)QuizEngine.start(l.quiz,document.getElementById('quizContainer'),levelId,id);},
 practice(){document.getElementById('view-practice').innerHTML=this.heading('PRACTICE / LEARN WITH YOUR HANDS','A small problem. A real skill.','Choose a task, work with a realistic dataset, and check your formula. Hints help you move forward without giving everything away.')+`<div class="filter-bar"><input id="practiceSearch" aria-label="Search exercises" placeholder="Search exercises…" oninput="Components.practiceRows()"><select id="practiceDataset" aria-label="Filter by dataset" onchange="Components.practiceRows()"><option value="">All datasets</option>${Object.entries(Datasets).map(([k,d])=>`<option value="${k}">${d.name}</option>`).join('')}</select><span class="filter-count">${Exercises.length} EXERCISES</span></div><div id="practiceRows"></div>`;this.practiceRows();},
 practiceRows(){const q=document.getElementById('practiceSearch').value.toLowerCase(),dataset=document.getElementById('practiceDataset').value,done=Store.read('xfa_exercises',{});const items=Exercises.filter(e=>(!dataset||e.dataset===dataset)&&(e.title+' '+e.topic).toLowerCase().includes(q));document.getElementById('practiceRows').innerHTML=items.map(e=>`<div class="activity-row"><span class="row-number">${done[e.id]?'✓':e.id.slice(3)}</span><div class="row-main"><strong>${escapeHTML(e.title)}</strong><p>${escapeHTML(e.instruction)}</p><span class="tag">${e.topic} · ${e.dataset}</span></div><button class="btn btn-sm" onclick="Lab.open('${e.id}')">Practice ${Icons.get('arrow')}</button></div>`).join('')||'<p class="empty-state">No exercises match. Try another skill or clear the dataset filter.</p>';},
 renderProjects(){document.getElementById('projectsContent').innerHTML=this.heading('PROJECT STUDIO','Turn a dataset into a decision.','Meaningful briefs. Realistic constraints. Build something you can explain and hand over.')+`<div class="filter-bar"><input id="projectSearch" aria-label="Search projects" placeholder="Search by project or skill…" oninput="Components.projectRows(this.value)"><span class="filter-count">${Projects.length} GUIDED PROJECTS</span></div><div class="project-grid" id="projectRows"></div>`;this.projectRows('');},
 projectRows(q){q=q.toLowerCase();document.getElementById('projectRows').innerHTML=Projects.filter(p=>(p.title+' '+p.skills.join(' ')).toLowerCase().includes(q)).map((p,i)=>`<article class="project-item"><div class="project-mark" aria-hidden="true">${Icons.get(i%3===0?'chart':i%3===1?'table':'query')}</div><div><p class="small">${p.level.toUpperCase()} / ${p.dataset.toUpperCase()}</p><h3><a href="#projects/${p.id}">${escapeHTML(p.title)}</a></h3><p>${escapeHTML(p.scenario)}</p><a class="text-link" href="#projects/${p.id}">Read the brief ${Icons.get('arrow')}</a></div></article>`).join('')||'<p class="empty-state">No projects found. Try “sales”, “report” or “lookup”.</p>';},
 project(id){const p=Projects.find(p=>p.id===id);if(!p)return this.renderProjects();const done=Store.read('xfa_projects',{})[id];document.getElementById('projectsContent').innerHTML=`<div class="project-detail"><a class="text-link" href="#projects">← Project studio</a>${this.heading('PROJECT BRIEF / '+p.level.toUpperCase(),escapeHTML(p.title),escapeHTML(p.scenario))}<div class="actions"><button class="btn btn-primary" onclick="App.projectWorkbook('${id}')">Open project workbook</button><button class="btn" onclick="Components.downloadDataset('${p.dataset}')">Download dataset CSV</button>${this.bookmarkBtn('project',id)}</div><section style="margin-top:28px"><h2>Your brief</h2><p>${escapeHTML(p.objective)}</p><p class="small">Skills: ${escapeHTML(p.skills.join(' · '))}</p><p class="small">Source: ${escapeHTML(Datasets[p.dataset].name)}. All records are fictional.</p></section><section><h2>Milestones</h2><ol>${p.steps.map(s=>`<li>${escapeHTML(s)}</li>`).join('')}</ol></section><details><summary>A useful starting point</summary><p>${escapeHTML(p.hint)}</p></details><details><summary>Solution checkpoint</summary><p>This formula verifies a key measure; your finished report should also meet the brief and reconcile to its source.</p><pre>${escapeHTML(p.solution)}</pre><p>Expected value: ${escapeHTML(exerciseExpected({dataset:p.dataset,solution:p.solution}))}</p></details><section><h2>Review & handover</h2><p>${escapeHTML(p.reflection)}</p><label class="sr-only" for="projectNote">Project reflection</label><textarea id="projectNote" placeholder="Record your result, assumptions and the checks you performed…" oninput="Store.saveNote('project-${id}',this.value)">${escapeHTML(Store.getNote('project-'+id))}</textarea><button class="btn btn-primary" style="margin-top:16px" onclick="App.saveProject('${id}')">${done?'✓ Project completed':'Mark project complete'}</button></section><section><h2>Take it further</h2><p>${escapeHTML(p.advanced)}</p><p class="small">Charts, PivotTables and Power Query steps run in Excel. Use the Lab to prepare data and verify supported formulas.</p></section></div>`;},
 downloadDataset(id){const d=Datasets[id];Lab.download(id+'-practice.csv',SheetEngine.csvWrite([d.headers,...d.rows]),'text/csv');},
 referenceTabs(active){return `<nav class="reference-tabs" aria-label="Reference sections">${[['formulas','Functions'],['shortcuts','Shortcuts'],['errors','Error guide']].map(([id,label])=>`<a href="#${id}" ${id===active?'aria-current="page"':''}>${label}</a>`).join('')}</nav>`;},
 renderFormulas(){document.getElementById('formulasContent').innerHTML=this.heading('REFERENCE / FUNCTIONS','The right function, within reach.','Syntax, examples and the details that make a formula work.')+this.referenceTabs('formulas')+`<div class="filter-bar"><input id="formulaSearch" aria-label="Search functions" placeholder="Search function name or purpose…" value="${escapeHTML(this.pendingFormula||'')}" oninput="Components.formulaRows()"><select id="formulaCategory" aria-label="Function category" onchange="Components.formulaRows()"><option value="">All categories</option>${[...new Set(Formulas.map(f=>f.category))].map(c=>`<option>${c}</option>`).join('')}</select></div><div id="formulaRows"></div>`;this.pendingFormula='';this.formulaRows();},
 formulaRows(){const q=document.getElementById('formulaSearch').value.toLowerCase(),category=document.getElementById('formulaCategory').value;document.getElementById('formulaRows').innerHTML=Formulas.filter(f=>(f.name+' '+f.desc).toLowerCase().includes(q)&&(!category||f.category===category)).map(f=>{const ex=Exercises.find(e=>e.topic===f.name);return `<details class="reference-row"><summary><span class="mono">${escapeHTML(f.name)}</span><p>${escapeHTML(f.desc)}</p><span class="badge">${f.difficulty}</span></summary><div class="reference-detail"><pre>${escapeHTML(f.syntax)}</pre><dl><dt>Arguments</dt><dd>${escapeHTML(f.args)}</dd><dt>Example</dt><dd><code>${escapeHTML(f.example)}</code></dd><dt>Watch for</dt><dd>${escapeHTML(f.error)}</dd><dt>Compatibility</dt><dd>${escapeHTML(f.compatibility||'Excel Microsoft 365, 2024, 2021, 2019 and 2016; check platform-specific behavior.')}</dd><dt>Related</dt><dd>${escapeHTML(Formulas.filter(x=>x.category===f.category&&x.name!==f.name).slice(0,3).map(x=>x.name).join(', ')||'See the related learning track.')}</dd></dl><div class="actions">${ex?`<button class="btn btn-primary" onclick="Lab.open('${ex.id}')">Open in Workbook Lab</button>`:'<span class="small">Practice this function in Excel.</span>'}<button class="btn" data-formula="${escapeHTML(f.example)}" onclick="App.copy(this.dataset.formula)">Copy example</button>${this.bookmarkBtn('formula',f.name)}</div></div></details>`;}).join('')||'<p class="empty-state">No functions match. Try “SUM”, “text” or “lookup”.</p>';},
 renderShortcuts(){document.getElementById('shortcutsContent').innerHTML=this.heading('REFERENCE / SHORTCUTS','Less reaching. More flow.','Start with a few high-value shortcuts. Excel shortcuts can differ from your browser and keyboard layout.')+this.referenceTabs('shortcuts')+`<div class="filter-bar"><input id="shortcutSearch" aria-label="Search shortcuts" placeholder="Search an action…" oninput="Components.shortcutRows()"><select id="shortcutPlatform" aria-label="Operating system" onchange="Components.shortcutRows()"><option value="win">Windows</option><option value="mac">Mac</option></select><select id="shortcutCategory" aria-label="Shortcut category" onchange="Components.shortcutRows()"><option value="">All categories</option>${[...new Set(ShortcutGuide.map(s=>s.category))].map(c=>`<option>${c}</option>`).join('')}</select></div><div class="table-scroll"><table class="data-table"><thead><tr><th>Action</th><th>Shortcut</th><th>Context</th></tr></thead><tbody id="shortcutRows"></tbody></table></div><p class="small" style="margin-top:20px">Workbook Lab supports arrow navigation, Shift selection, Enter/F2 editing, clipboard commands, undo/redo and fill down. Other listed shortcuts apply to Excel.</p><a class="btn" href="#lab">Practice supported shortcuts</a>`;this.shortcutRows();},
 shortcutRows(){const q=document.getElementById('shortcutSearch').value.toLowerCase(),platform=document.getElementById('shortcutPlatform').value,category=document.getElementById('shortcutCategory').value;document.getElementById('shortcutRows').innerHTML=ShortcutGuide.filter(s=>s.action.toLowerCase().includes(q)&&(!category||s.category===category)).map(s=>`<tr><td><strong>${s.action}</strong><br><small>${s.category} · ${s.importance}</small></td><td><kbd>${s[platform]}</kbd></td><td>${s.context}</td></tr>`).join('')||'<tr><td colspan="3">No shortcuts found. Try another action or category.</td></tr>';},
 errors(){document.getElementById('view-errors').innerHTML=this.heading('REFERENCE / ERROR GUIDE','An error is a clue.','Read the message, find the cause, and fix the underlying calculation.')+this.referenceTabs('errors')+ErrorGuide.map(([code,meaning,cause,example,fix])=>`<details class="reference-row"><summary><span class="mono">${code}</span><p>${meaning}</p><span class="badge">Diagnose</span></summary><div class="reference-detail"><p><strong>Typical cause:</strong> ${cause}</p><pre>${escapeHTML(example)}</pre><p><strong>Diagnose & fix:</strong> ${fix}</p><button class="btn" onclick="Components.errorPractice('${code}')">Try an error exercise</button></div></details>`).join('');},
 errorPractice(code){const exercise=({'#DIV/0!':'ex-020','#N/A':'ex-031','#VALUE!':'ex-029','#REF!':'ex-015','#NAME?':'ex-001','#NUM!':'ex-007','#NULL!':'ex-001','#SPILL!':'ex-001','#CALC!':'ex-001'})[code];if(['#SPILL!','#CALC!','#NULL!'].includes(code)){App.dialog('Practice in Excel','<p>This error concerns array or intersection behavior outside the Lab subset. In Excel, use the example shown in the error guide, inspect the affected range, then apply the diagnosis. The Lab reports unsupported array results as #SPILL! and does not simulate dynamic arrays.</p>');}else Lab.open(exercise);},
 savedItems(items){return items.map(b=>{let title=b.id,href='#progress';if(b.type==='lesson'){const l=getAllLessons().find(l=>l.id===b.id);if(l){title=l.title;href=`#learn/${l.levelId}/${l.id}`;}}else if(b.type==='project'){title=Projects.find(p=>p.id===b.id)?.title||title;href='#projects/'+b.id;}else if(b.type==='formula')href='#formulas';else if(b.type==='level'){title=getLevel(b.id)?.title||title;href='#learn/'+b.id;}return `<a class="lesson-link" href="${href}">${escapeHTML(title)}<small>${b.type}</small></a>`;}).join('');},
 renderProgress(){const {total,done}=this.counts(),bookmarks=Store.getBookmarks(),notes=Object.entries(Store.getNotes()).filter(([,v])=>v.trim()),projects=Object.keys(Store.read('xfa_projects',{}));document.getElementById('progressContent').innerHTML=this.heading('YOUR LEARNING RECORD','Skills grow through practice.',`${done} of ${total} lessons completed · ${Object.keys(Store.read('xfa_exercises',{})).length} exercises checked · ${projects.length} projects completed`)+`<div>${AcademyTracks.map(t=>`<div class="skill-row"><a href="#learn/${t.id}">${escapeHTML(t.title)}</a><div class="progress-bar" role="progressbar" aria-label="${escapeHTML(t.title)}" aria-valuenow="${this.done(t)}" aria-valuemin="0" aria-valuemax="${t.lessons.length}"><div class="progress-fill" style="width:${this.done(t)/t.lessons.length*100}%"></div></div><small>${this.done(t)} / ${t.lessons.length}</small></div>`).join('')}</div><div class="section-heading"><h2>Saved for later</h2></div>${bookmarks.length?this.savedItems(bookmarks):'<p class="empty-state">Your reading list starts with one bookmark. Save a lesson, function or project to find it here.</p>'}<div class="section-heading"><h2>Your notes</h2></div>${notes.length?notes.map(([id,text])=>`<details class="reference-row"><summary>${escapeHTML(getAllLessons().find(l=>l.id===id)?.title||Projects.find(p=>'project-'+p.id===id)?.title||id)}</summary><p style="white-space:pre-wrap">${escapeHTML(text)}</p></details>`).join(''):'<p class="empty-state">A useful note beats a perfect memory. Add a note beneath any lesson.</p>'}<div class="section-heading"><h2>Completed projects</h2></div>${projects.length?projects.map(id=>`<a class="lesson-link" href="#projects/${id}">${escapeHTML(Projects.find(p=>p.id===id)?.title||id)}<span>✓</span></a>`).join(''):'<p class="empty-state">Choose a project, work through its milestones, and mark it complete after your review.</p>'}`;},
 renderSettings(){const s=Store.getSettings();document.getElementById('settingsContent').innerHTML=this.heading('PREFERENCES','Make this space work for you.')+`<div class="settings-row"><div><strong>Appearance</strong><p>Choose light, dark, or follow your system.</p></div><label><span class="sr-only">Color theme</span><select onchange="Store.saveSettings({theme:this.value});App.applyTheme()">${['light','dark','system'].map(t=>`<option value="${t}" ${s.theme===t?'selected':''}>${t[0].toUpperCase()+t.slice(1)}</option>`).join('')}</select></label></div><div class="settings-row"><div><strong>Focus timer</strong><p>A gentle time boundary, never a deadline.</p></div><label><span class="sr-only">Focus timer duration</span><select onchange="Store.saveSettings({focusTimer:Number(this.value)})">${[0,5,10,15,25].map(n=>`<option value="${n}" ${(s.focusTimer??25)===n?'selected':''}>${n?n+' minutes':'Off'}</option>`).join('')}</select></label></div><div class="settings-row"><div><strong>Low energy mode</strong><p>Keep the next action visible and reduce secondary content.</p></div><label><input type="checkbox" ${s.lowEnergy?'checked':''} onchange="Store.saveSettings({lowEnergy:this.checked});App.applyEnergy()">Enable</label></div><div class="settings-row"><div><strong>Your learning data</strong><p>Saved in this browser. No account or server sync.</p></div><button class="btn" onclick="App.exportProgress()">Export progress</button></div><div class="settings-row"><div><strong>Workbook backup</strong><p>Export formulas, sheets and formatting as JSON from the Lab.</p></div><a class="btn" href="#lab">Open Lab</a></div><p class="small" style="margin-top:24px">Changing browser profiles, clearing site data or using a different site address can create a separate learning record. Keep a workbook export for work you want to retain.</p>`;},
 assessment(){document.getElementById('view-assessment').innerHTML=this.heading('FIND YOUR STARTING POINT','Start where it feels useful.','This is a recommendation, not a gate. Every track remains open.')+`<div class="activity-row"><div class="row-main"><strong>I’m new to spreadsheets.</strong><p>Start with cells, navigation and entering data.</p></div><a class="btn btn-primary" href="#learn/track-fundamentals">Start here</a></div><div class="activity-row"><div class="row-main"><strong>I know the grid, but formulas feel uncertain.</strong><p>Build confidence with references, calculations and conditional logic.</p></div><a class="btn" href="#learn/track-formula-foundations">Explore formulas</a></div><div class="activity-row"><div class="row-main"><strong>I use Excel and want stronger analysis.</strong><p>Move into data quality, reporting and professional workflows.</p></div><a class="btn" href="#learn/track-data-quality">Explore analysis</a></div><div class="section-heading"><h2>Try a quick checkpoint</h2></div><div id="quizContainer"></div>`;QuizEngine.start({question:'You copy =B2*$F$2 down one row. Which formula should appear?',options:['=B3*$F$2','=B3*$F$3','=B2*$F$2'],correct:0,explanation:'B2 moves with the row, while both parts of $F$2 remain fixed. If this felt unfamiliar, start with Formula foundations.'},document.getElementById('quizContainer'),'assessment','references');}
};
